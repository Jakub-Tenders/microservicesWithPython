# Module 4: Messaging infrastructure only (Kafka + RabbitMQ)
# Services still run locally with uvicorn + SQLite.
# Kafka and RabbitMQ are provided by docker-compose.infra.yml.
#
# Start with:
#   docker compose -f docker-compose.infra.yml up -d rabbitmq zookeeper kafka
#
# No service containers in this module.
version: "3.9"
